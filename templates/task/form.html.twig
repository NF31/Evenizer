
{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{ title }}</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">Créer</button>
    {{ form_end(form) }}
    <a href="{{ path('task_list') }}" class="afficher">Afficher la liste</a>

{% endblock %}